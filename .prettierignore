# Build output
.next/
out/
build/
dist/

# Dependencies
node_modules/

# Lockfiles
package-lock.json
pnpm-lock.yaml
yarn.lock

# Generated
next-env.d.ts
coverage/
*.tsbuildinfo

# Written by the Payload CLI, not by hand. `payload generate:importmap` runs as
# part of `npm run build` and `generate:types` rewrites payload-types.ts, and
# neither output is Prettier-formatted — so format-gating them makes any build
# fail the next format:check.
src/app/(payload)/admin/importMap.js
src/payload-types.ts
