Drop the Season VF-Trial font here as:
  season-vf.woff2

(.ttf/.otf also fine — update the @font-face url + format in src/app/globals.css)
Wired up in globals.css as the site-wide --font-sans / --font-display.
